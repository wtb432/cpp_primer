(a)legal,deliver sp into process will increase the refference of the address.

(b)illegal,plain pointer can't convert to smart pointer implicitly.

(c)illegal,plain pointer can't convert to smart pointer implicitly.

(d)illegal,when process returned the raw point pointed memory will be destroyed,so you may
get a warn <<double free>>

